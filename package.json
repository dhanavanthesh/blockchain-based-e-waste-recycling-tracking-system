{
  "name": "ewaste-tracking",
  "version": "2.0.0",
  "description": "E-waste tracking blockchain application with simplified setup",
  "scripts": {
    "start": "node scripts/start.js",
    "stop": "node scripts/stop.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../blockchain && npm install"
  },
  "keywords": ["blockchain", "e-waste", "tracking", "ethereum", "ganache"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0",
    "tree-kill": "^1.2.2"
  }
}
